import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  label: {
    fontSize: 12,
    fontWeight: '600',
    lineHeight: 20,
    marginBottom: 5
  }
});

export default styles;
